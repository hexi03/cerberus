@model IList<cerberus.DTO.ProductionRegistryItem>


<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                Name
            </th>

            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.production_item.name)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.production_item.id }) |
                @Html.ActionLink("Details", "Details", new { id=item.production_item.id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.production_item.id })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
